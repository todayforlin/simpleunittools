---
const title = "Celsius to Fahrenheit Converter";
const description =
  "Convert Celsius to Fahrenheit easily. Includes an interactive calculator, simple formula, conversion table, and real-world examples.";
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />

    <style>
      :root{
        --bg: #f5f7fb;
        --card: #ffffff;
        --text: #0f172a;
        --muted: #64748b;
        --line: #e5e7eb;
        --primary: #2b6cb0;
        --primary-2: #1e4e8c;
        --soft: #eef6ff;
        --soft-2: #d9ecff;
        --shadow: 0 6px 18px rgba(15, 23, 42, .08);
        --radius: 12px;
        --radius-sm: 10px;
      }

      *{ box-sizing: border-box; }
      body{
        margin: 0;
        font-family: system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial;
        background: var(--bg);
        color: var(--text);
      }

      .wrap{
        max-width: 980px;
        margin: 22px auto;
        padding: 0 16px 32px;
      }

      .page-title{
        display:flex;
        align-items:center;
        gap: 10px;
        font-size: 20px;
        font-weight: 700;
        margin: 6px 0 14px;
      }
      .page-title .icon{
        width: 28px;
        height: 28px;
        border-radius: 8px;
        background: linear-gradient(135deg, var(--primary), #4aa3ff);
        display:flex;
        align-items:center;
        justify-content:center;
        color:#fff;
        font-weight:800;
      }
      .subtitle{
        margin:0 0 14px;
        color:var(--muted);
        font-size:14px;
      }

      .card{
        background:#fff;
        border:1px solid var(--line);
        border-radius:var(--radius);
        box-shadow:var(--shadow);
        overflow:hidden;
      }

      .tabs{
        display:flex;
        border-bottom:1px solid var(--line);
        padding:10px 10px 0;
        overflow-x:auto;
      }
      .tab{
        border:1px solid var(--line);
        border-bottom:none;
        background:#f8fafc;
        padding:10px 14px;
        border-top-left-radius:10px;
        border-top-right-radius:10px;
        font-weight:650;
        cursor:pointer;
        margin-right:8px;
      }
      .tab[aria-selected="true"]{
        background:#fff;
        color:var(--primary-2);
      }

      .panel{ padding:16px; }
      [data-panel]{ display:none; }
      [data-panel].active{ display:block; }

      .option-block{
        border:1px solid var(--line);
        border-radius:var(--radius-sm);
        padding:14px;
      }
      .option-title{
        display:flex;
        justify-content:space-between;
        margin-bottom:10px;
      }
      .hint{ font-size:12px;color:var(--muted); }

      .radio-row{
        display:flex;
        gap:10px;
        padding:10px;
        cursor:pointer;
      }

      .row{
        display:flex;
        gap:12px;
        flex-wrap:wrap;
        align-items:center;
      }

      .field{
        display:flex;
        flex-direction:column;
        gap:6px;
        min-width:220px;
      }
      .input{
        border:1px solid var(--line);
        border-radius:10px;
        padding:10px 12px;
      }

      .btn{
        border:1px solid var(--line);
        background:#fff;
        padding:10px 14px;
        border-radius:10px;
        cursor:pointer;
      }
      .btn.primary{
        background:var(--soft);
        color:#0b3b76;
      }

      .result{
        margin-top:14px;
        border:1px solid var(--line);
        border-radius:var(--radius-sm);
      }
      .result-head{
        background:var(--soft-2);
        padding:10px;
        font-weight:800;
      }
      .result-body{ padding:12px; }

      .pill{
        display:inline-block;
        padding:8px 10px;
        background:#f0f7ff;
        border-radius:10px;
        font-weight:650;
      }

      .table{
        width:100%;
        border-collapse:collapse;
        margin-top:10px;
      }
      .table th,.table td{
        border:1px solid var(--line);
        padding:10px;
      }
      .table th{ background:#f8fafc; }

      .muted{ color:var(--muted);font-size:13px; }
      .foot{ margin-top:16px;display:flex;justify-content:space-between; }
    </style>
  </head>

  <body>
    <main class="wrap">
      <div class="page-title">
        <div class="icon">≡</div>
        <div>°C ↔ °F Converter</div>
      </div>
      <p class="subtitle">
        Convert between Celsius and Fahrenheit using standard temperature formulas.
      </p>

      <section class="card">
        <div class="tabs">
          <button class="tab" aria-selected="true" data-tab="converter">Converter</button>
          <button class="tab" data-tab="formula">Formula</button>
          <button class="tab" data-tab="table">Table</button>
          <button class="tab" data-tab="examples">Examples</button>
          <button class="tab" data-tab="faq">FAQ</button>
        </div>

        <div class="panel">
          <div data-panel="converter" class="active">
            <div class="option-block">
              <div class="option-title">
                <h2>Interactive Converter</h2>
                <div class="hint">Pick a mode, then enter a value</div>
              </div>

              <label class="radio-row">
                <input type="radio" name="mode" value="c2f" checked />
                Celsius → Fahrenheit
              </label>
              <label class="radio-row">
                <input type="radio" name="mode" value="f2c" />
                Fahrenheit → Celsius
              </label>
              <label class="radio-row">
                <input type="radio" name="mode" value="both" />
                Two-way
              </label>

              <div class="row">
                <div class="field">
                  <label>°C (Celsius)</label>
                  <input id="cInput" class="input" type="number" />
                </div>
                <div class="field">
                  <label>°F (Fahrenheit)</label>
                  <input id="fInput" class="input" type="number" />
                </div>
                <button id="clearBtn" class="btn">Clear</button>
              </div>
            </div>

            <div class="result">
              <div class="result-head">Result</div>
              <div class="result-body">
                <span id="modeLabel" class="pill">°C → °F</span>
              </div>
            </div>
          </div>

          <div data-panel="formula">
            <div class="option-block">
              <div class="pill">°F = °C × 9/5 + 32</div><br /><br />
              <div class="pill">°C = (°F − 32) × 5/9</div>
            </div>
          </div>

          <div data-panel="table">
            <div class="option-block">
              <table class="table">
                <tr><th>°C</th><th>°F</th></tr>
                <tr><td>0</td><td>32</td></tr>
                <tr><td>20</td><td>68</td></tr>
                <tr><td>37</td><td>98.6</td></tr>
                <tr><td>100</td><td>212</td></tr>
              </table>
            </div>
          </div>

          <div data-panel="examples">
            <div class="option-block">
              <ul>
                <li>Room temperature ≈ 20–25°C (68–77°F)</li>
                <li>Human body temperature ≈ 37°C (98.6°F)</li>
                <li>Water boils at 100°C (212°F)</li>
              </ul>
            </div>
          </div>

          <div data-panel="faq">
            <div class="option-block">
              <p><strong>Why add 32?</strong> Fahrenheit scale is offset from Celsius.</p>
              <p><strong>Which is used in the US?</strong> Fahrenheit.</p>
            </div>
          </div>

          <div class="foot">
            <a href="/">← Back to Home</a>
            <span class="muted">SEO-safe</span>
          </div>
        </div>
      </section>
    </main>

    <script>
      const cInput = document.getElementById("cInput");
      const fInput = document.getElementById("fInput");
      const radios = document.querySelectorAll("input[name=mode]");
      const modeLabel = document.getElementById("modeLabel");
      const clearBtn = document.getElementById("clearBtn");

      let updating = false;

      function mode(){
        return [...radios].find(r=>r.checked).value;
      }

      function c2f(c){ return c * 9/5 + 32; }
      function f2c(f){ return (f - 32) * 5/9; }

      cInput.addEventListener("input",()=>{
        if(updating || mode()==="f2c") return;
        updating=true;
        fInput.value = c2f(parseFloat(cInput.value)||0).toFixed(2);
        updating=false;
      });

      fInput.addEventListener("input",()=>{
        if(updating || mode()==="c2f") return;
        updating=true;
        cInput.value = f2c(parseFloat(fInput.value)||0).toFixed(2);
        updating=false;
      });

      radios.forEach(r=>r.addEventListener("change",()=>{
        modeLabel.textContent =
          r.value==="c2f"?"°C → °F":r.value==="f2c"?"°F → °C":"Two-way";
        cInput.disabled = r.value==="f2c";
        fInput.disabled = r.value==="c2f";
      }));

      clearBtn.onclick=()=>{
        cInput.value="";
        fInput.value="";
      };
    </script>
  </body>
</html>
