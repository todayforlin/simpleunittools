---
const title = "AI赚钱 - cases";
const description = "AI赚钱专题栏目：cases。";
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={new URL(Astro.url.pathname, Astro.site).toString()} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={new URL(Astro.url.pathname, Astro.site).toString()} />
    <meta property="og:type" content="website" />
    <style>body{font-family:system-ui,sans-serif;max-width:900px;margin:2rem auto;padding:0 1rem;line-height:1.7}.muted{color:#555}</style>
  </head>
  <body>
    <h1>{title}</h1>
    <p class="muted">该栏目骨架已创建，下一步将填充首批内容与关键词集群。</p>
    <p><a href="/ai-money/">← 返回 AI赚钱总览</a></p>
  </body>
</html>
